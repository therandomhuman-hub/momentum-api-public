import { readFileSync, existsSync } from "node:fs";

const required=["CLAUDE.md","docs/ARCHITECTURE.md","docs/ENGINEERING-CHECKLIST.md","docs/PLAN-V2.md","skills/secure-saas-build/SKILL.md","demo/index.html","auth/src/index.ts","worker/src/index.ts","worker/migrations/0022_free_only.sql","openapi.yaml"];
for(const path of required)if(!existsSync(path))throw new Error(`Missing required project artifact: ${path}`);
const index=readFileSync("demo/index.html","utf8"),claude=readFileSync("CLAUDE.md","utf8"),architecture=readFileSync("docs/ARCHITECTURE.md","utf8"),auth=readFileSync("auth/src/index.ts","utf8"),gateway=readFileSync("worker/src/index.ts","utf8"),migration=readFileSync("worker/migrations/0022_free_only.sql","utf8"),openapi=readFileSync("openapi.yaml","utf8");
const versionContract=gateway.match(/\{name:"Momentum API",version:"([^"]+)",engine:"([^"]+)",auth:"([^"]+)",access:"([^"]+)"\}/);if(!versionContract)throw new Error("Gateway version contract is missing");
const[,apiVersion,engineVersion,authVersion,accessMode]=versionContract;
const assertions=[
 [index.includes("Sign in with Google"),"demo must expose Google sign-in"],[index.includes("100% FREE"),"demo must present free-only access"],[index.includes("No Pro tier"),"demo must explain there is no Pro tier"],[!index.toLowerCase().includes("razorpay"),"demo must not contain payment-provider UI"],[!index.includes("Pro"),"demo must not contain a Pro product tier"],
 [!gateway.toLowerCase().includes("razorpay"),"gateway must not contain payment-provider routes"],[!gateway.includes("BILLING"),"gateway must not bind a billing worker"],[gateway.includes("access:\"gmail-free\""),"gateway must expose free-only access mode"],[gateway.includes("/auth/health"),"gateway must expose auth health"],
 [auth.includes("normalizeGmail"),"auth must validate Gmail addresses"],[!auth.includes("google_pro_accounts"),"auth must not contain a Pro access list"],[auth.includes("google_free_accounts"),"auth must use the Free Gmail access list"],[!auth.includes("/admin/pro/grant"),"auth must not expose Pro grant"],[!auth.includes("/admin/pro/revoke"),"auth must not expose Pro revoke"],
 [migration.includes("UPDATE customers"),"free-only migration must normalize customers"],[migration.includes("DELETE FROM plans WHERE tier <> 'free'"),"free-only migration must remove other plans"],[migration.includes("DROP TABLE IF EXISTS google_pro_accounts"),"free-only migration must remove Pro access table"],
 [claude.includes("Every outbound network call has a bounded timeout"),"reliability rule missing from CLAUDE.md"],[claude.includes("Make retryable mutations idempotent"),"idempotency rule missing from CLAUDE.md"],[architecture.includes("Gmail"),"architecture must document Gmail access"],[openapi.includes(`version: ${apiVersion}`),"OpenAPI version must match gateway"],[openapi.includes("Gmail-only"),"OpenAPI must document Gmail access"],[!openapi.includes("/admin/pro/grant"),"OpenAPI must not document Pro grants"],[!openapi.includes("/admin/pro/revoke"),"OpenAPI must not document Pro revokes"],[accessMode==="gmail-free","gateway access mode must be gmail-free"]
];
for(const[ok,message]of assertions)if(!ok)throw new Error(message);
console.log(`Momentum engineering contract checks passed (API ${apiVersion}, engine ${engineVersion}, auth ${authVersion}, access ${accessMode}).`);
