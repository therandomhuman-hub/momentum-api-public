name: Production Health Checks

on:
  workflow_dispatch: {}
  schedule:
    - cron: "17 * * * *"

permissions:
  contents: read

jobs:
  health:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check public gateway
        shell: bash
        run: |
          set -euo pipefail
          BASE="https://momentum-api-public.manikandanruki2004.workers.dev"
          request() { curl --connect-timeout 10 --max-time 30 --silent --show-error --retry 3 --retry-delay 2 "$@"; }
          request "$BASE/health" | tee /tmp/gateway.json
          jq -e '.status == "ok" and .service == "momentum-api-public" and .engine == "service-binding-ok" and .auth == "service-binding-ok" and .access == "gmail-free" and .usage == "unlimited" and .rate_limit_per_minute == 10 and .max_results == 20' /tmp/gateway.json >/dev/null
          for header in strict-transport-security x-content-type-options referrer-policy permissions-policy content-security-policy cross-origin-opener-policy; do
            request --head "$BASE/health" | tr -d '\r' | grep -i "^${header}:" >/dev/null
          done
          request --head "$BASE/health" | tr -d '\r' | grep -Eiq "^content-security-policy: .*frame-ancestors 'none'" || { echo "CSP frame-ancestors missing"; exit 1; }

      - name: Check auth service binding
        shell: bash
        run: |
          set -euo pipefail
          BASE="https://momentum-api-public.manikandanruki2004.workers.dev"
          STATUS="$(curl --connect-timeout 10 --max-time 30 --silent --show-error --output /tmp/auth-binding.json --write-out '%{http_code}' --retry 3 --retry-delay 2 "$BASE/auth/health")"
          test "$STATUS" = "200"
          jq -e '.status == "ok" and .service == "momentum-auth" and .mode == "gmail-free"' /tmp/auth-binding.json >/dev/null

      - name: Check unauthenticated boundaries
        shell: bash
        run: |
          set -euo pipefail
          BASE="https://momentum-api-public.manikandanruki2004.workers.dev"
          for path in /auth/me /v1/me /v1/momentum; do
            STATUS="$(curl --connect-timeout 10 --max-time 30 --silent --show-error --output /tmp/boundary.json --write-out '%{http_code}' --retry 3 --retry-delay 2 "$BASE$path")"
            test "$STATUS" = "401"
            jq -e '.error.code == "UNAUTHORIZED" and (.error.request_id | type == "string" and length > 0)' /tmp/boundary.json >/dev/null
          done

      - name: Check method and query validation
        shell: bash
        run: |
          set -euo pipefail
          BASE="https://momentum-api-public.manikandanruki2004.workers.dev"
          STATUS="$(curl --connect-timeout 10 --max-time 30 --silent --show-error --output /tmp/query.json --write-out '%{http_code}' --retry 3 --retry-delay 2 "$BASE/v1/momentum?min_stars=-1")"
          test "$STATUS" = "400"
          jq -e '.error.code == "INVALID_QUERY" and (.error.request_id | type == "string" and length > 0)' /tmp/query.json >/dev/null
          STATUS="$(curl --connect-timeout 10 --max-time 30 --silent --show-error --output /tmp/method.json --write-out '%{http_code}' --retry 3 --retry-delay 2 -X PUT "$BASE/health")"
          test "$STATUS" = "405"
          jq -e '.error.code == "METHOD_NOT_ALLOWED" and (.error.request_id | type == "string" and length > 0)' /tmp/method.json >/dev/null
          STATUS="$(curl --connect-timeout 10 --max-time 30 --silent --show-error --output /tmp/limit.json --write-out '%{http_code}' --retry 3 --retry-delay 2 "$BASE/v1/momentum?limit=21")"
          test "$STATUS" = "400"
          jq -e '.error.code == "INVALID_QUERY"' /tmp/limit.json >/dev/null

      - name: Check retired payment route stays removed
        shell: bash
        run: |
          set -euo pipefail
          BASE="https://momentum-api-public.manikandanruki2004.workers.dev"
          STATUS="$(curl --connect-timeout 10 --max-time 30 --silent --show-error --output /tmp/notfound.json --write-out '%{http_code}' --retry 3 --retry-delay 2 "$BASE/billing/status")"
          test "$STATUS" = "404"
          jq -e '.error.code == "NOT_FOUND"' /tmp/notfound.json >/dev/null
